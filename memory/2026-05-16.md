# 2026-05-16 周六

## 酒量测试 App — 编译阻塞清零，CI 已触发

### 🔧 总管修复（编译阻塞）
1. DataStack enum→class (enum 不能存 `let container`)
2. DashboardView bacGaugeSection: 补 BACGaugeArc() shape + VStack 包裹
3. DashboardView NavigationLink 拼写
4. BACGauge 进度弧缺 Circle()
5. View init() 全量: DrinkLoggerView/SoberTestView/HistoryView/ProfileView 补 init()，SettingsView 用默认值
6. AlcoholToleranceApp.swift: 加 NavigationStack 包裹（否则 NavigationLink 不跳转），ModelContext→container.mainContext

### 🔍 reviewer 修复
7. **P0-1**: BACResult.level → `@Transient` 标记
8. **P1-1**: SettingsView 酒驾阈值: duiLimit→drinkDriveLimit，标签改 "饮酒驾驶"/"醉酒驾驶"
9. **P1-3**: User.title 重复调用缓存

### 💻 developer 修复
10. 4 个 #Preview 块: 自定义 init() 抑制 memberwise init → 无参 Preview 编译通过

### 🧪 tester 交付
11. 审阅 6 个现有测试文件，新增 5 个测试文件，总计 ~216 用例
    - 发现 2 个 drivingAdvice 测试期望值问题（对齐 LegalRegion 实现）→ 已修
    - 新增: ModelsTests, ServicesTests, IntegrationTests(10 端到端), ViewModelTests, PerformanceTests
12. 项目测试文件从 6→11，用例从 ~100→216

### 🚀 CI & Appetize.io
- GitHub CI: `AlcohocToleranceApp/.github/workflows/ios-ci.yml`
- 流程: xcodegen → xcodebuild → 测试 → 打包 → Appetize 上传 → Discord 通知
- 需 GitHub Secret: `APPETIZE_API_TOKEN`
- Repo: https://github.com/jakkierong11-tech/AlcoholToleranceApp
- CI: https://github.com/jakkierong11-tech/AlcoholToleranceApp/actions

### ⚠️ 待办
- 🎨 designer 未响应（UI 审查尚未完成，非编译阻塞）
- Appetize API token 需老板配置
- CI 构建结果待确认
