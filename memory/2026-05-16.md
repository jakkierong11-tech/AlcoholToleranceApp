# 2026-05-16 周六

## 酒量测试 App — 编译阻塞清零，准备上 Appetize.io

### 总管修复（编译阻塞）
1. DataStack enum→class (enum 不能存 `let container`)
2. DashboardView bacGaugeSection: 补 BACGaugeArc() shape + VStack 包裹
3. DashboardView NavigationLink 拼写
4. BACGauge 进度弧缺 Circle()
5. View init() 全量: DrinkLoggerView/SoberTestView/HistoryView/ProfileView 补 init()，SettingsView 用默认值
6. AlcoholToleranceApp.swift: 加 NavigationStack 包裹（否则 NavigationLink 不跳转），ModelContext→container.mainContext

### 🔍 reviewer 修复
7. **P0-1**: BACResult.level → `@Transient` 标记，SwiftData 不观察 computed property
8. **P1-1**: SettingsView 酒驾阈值: duiLimit→drinkDriveLimit，标签改 "饮酒驾驶"/"醉酒驾驶"
9. **P1-3**: User.title 重复调用缓存

### 💻 developer 修复
10. 4 个 #Preview 块: 自定义 init() 抑制了 memberwise init，Preview 改用无参调用

### 📊 测试覆盖
- ModelsTests.swift: DrinkRecord(11) + User(12) + BACResult(4) + DrinkSession(3) + LegalRegion(11) + BACLevel(4) + DrinkType(4) = 49 用例
- BACCalculator 6 文件: ~50 用例
- 总计约 100 个测试用例
- 缺失: DrinkSessionManager/UserManager CRUD 测试（非阻塞）

### 🚀 Appetize.io
- **走 GitHub CI**（已有 ios-ci.yml）：macos 上 xcodegen → xcodebuild → 打包 → 上传 Appetize.io API
- CI 触发分支改 master（原 main）
- Appetize 上传需 GitHub Secret: `APPETIZE_API_TOKEN`
- CI 成功后会 Discord webhook 通知，通知里带 Appetize 链接
